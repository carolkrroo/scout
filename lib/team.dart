import 'package:flutter/material.dart';

class Team {
  String name;
  Color shirtColor;
  Color shortsColor;
  Color sockColor;

  Team(
      {@required this.name, this.shirtColor, this.shortsColor, this.sockColor});
}
